@{
    var randomSize = model[0] == null ? 100 : model[0]
    var minSize = model[1] == null ? 200 : model[1]
    var height = Math.floor(Math.random() * randomSize + minSize);
}

<img src="https://picsum.photos/1000/@height" alt="Advertisement" style="width: 100%" />
